[advisories]
ignore = [
    # RUSTSEC-2023-0071: Marvin Attack in the `rsa` crate (no fix available).
    # `rsa` is pulled in transitively by sqlx-macros-core which includes all
    # database drivers even when only PostgreSQL is configured (sqlx `any`
    # feature). QShield does not use MySQL or RSA anywhere; this is dead code.
    "RUSTSEC-2023-0071",
]
